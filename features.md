- hide annotations command
- add layer to snapping exclusion command
- autosave
- import styles, expressions, scripts, etc
- use Kart as undo/redo backend
  - look into bundled installation
  - look into running shell commands from Python
- set CAD layers as private, create a "recover" feature
